[project]
name = "enterprise-ai-performance-coach"
version = "0.1.0"
description = "Synthetic enterprise RAG and performance-support reference implementation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
